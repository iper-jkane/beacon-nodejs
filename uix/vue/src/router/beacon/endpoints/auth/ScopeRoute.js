import AuthScope from '../../../../views/AuthScope.vue'

const AuthScopeRoute = {
    path: '/auth/scope',
    name: 'authScope',
    components: {
      AuthScope: AuthScope,
    },
}

export { AuthScopeRoute } 

